require "spec_helper"

describe LgtmCreator do
  it "has a version number" do
    expect(LgtmCreator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
